export default function CircularGradient({ className = '' }: { className?: string }) {
    return (
        <div className={`rounded-full bg-[#F8B78D] opacity-20 blur-3xl ${className}`}>

        </div>
    );
}